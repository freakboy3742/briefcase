When running an app, Briefcase now prioritizes the exit code reported by the app itself (via its exit sentinel) over the exit status of the app's process. This fixes an intermittent failure where an app that reported successful completion, but was still shutting down when Briefcase's grace period expired, would be forcibly terminated and have that termination misreported as the app failing.
